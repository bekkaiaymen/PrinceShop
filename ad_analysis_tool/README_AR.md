# 🎯 محلل إعلانات Facebook - الحل الرسمي

## 📁 الملفات

### ✅ الطريقة الرسمية (الموصى بها)
- **`facebook_api_analyzer.py`** - محلل يستخدم Facebook Graph API الرسمي
- **`api_config.json`** - ملف التكوين (يحفظ Access Token)
- **`API_GUIDE.md`** - دليل شامل للحصول على Access Token

### ⚠️ الطريقة القديمة (غير موصى بها)
- **`facebook_feed_scanner.py`** - يستخدم web scraping (قد يتعطل)
- **`ad_library_analyzer.py`** - يستخدم web scraping (لا يعمل حالياً)
- **`config.json`** - تكوين الطريقة القديمة

---

## 🚀 البدء السريع (5 دقائق)

### 1️⃣ احصل على Access Token

افتح هذا الرابط:
```
https://developers.facebook.com/tools/explorer/
```

1. اضغط "Add a Permission" → ابحث عن `ads_read` → فعّله
2. اضغط "Generate Access Token"
3. انسخ التوكن الطويل

للمزيد من التفاصيل، اقرأ [API_GUIDE.md](API_GUIDE.md)

### 2️⃣ شغّل البرنامج

```powershell
python facebook_api_analyzer.py
```

### 3️⃣ أدخل البيانات

- **الصق التوكن** (سيُحفظ تلقائياً)
- **اكتب المنتج**: مثل "سماعات بلوتوث"
- **عدد الإعلانات**: مثل 100

---

## 📊 النتائج

سيتم إنشاء ملفين CSV:

1. **`facebook_ads_TIMESTAMP.csv`**
   - الإعلانات الناجحة فقط ✅
   - مع روابطها المباشرة 🔗

2. **`facebook_ads_TIMESTAMP_all.csv`**
   - جميع الإعلانات (ناجحة + عادية) 📋

---

## ✨ المميزات

| الميزة | الطريقة الرسمية (API) | Web Scraping القديم |
|--------|----------------------|---------------------|
| السرعة | ⚡ سريع جداً | 🐌 بطيء |
| الموثوقية | ✅ مستقر 100% | ❌ يتعطل كثيراً |
| القيود | ✅ لا قيود | ⚠️ قد تُحظر |
| الدقة | ✅ دقيق تماماً | ⚠️ أخطاء متكررة |
| المدة | ✅ يدوم شهرين | ❌ قد يتوقف |

---

## 📝 معايير النجاح

الإعلان يُعتبر **ناجح** إذا:
- أكثر من **1000 مشاهدة** لكل دولار منفق
- أو أكثر من **10,000 مشاهدة** إجمالاً

---

## ⚙️ إعدادات متقدمة

### تغيير الدولة المستهدفة
عدّل في `api_config.json`:
```json
"default_country": "DZ"
```

الدول المتاحة:
- `DZ` - الجزائر
- `EG` - مصر  
- `SA` - السعودية
- `MA` - المغرب
- `TN` - تونس

### تغيير معايير النجاح
عدّل في `api_config.json`:
```json
"success_criteria": {
    "min_impressions_per_dollar": 1000,
    "min_total_impressions": 10000
}
```

---

## ❓ حل المشاكل

### ❌ "Invalid OAuth access token"
- التوكن منتهي → احصل على واحد جديد
- التوكن خاطئ → تأكد من نسخه كاملاً

### ❌ "Permissions error"  
- التوكن لا يملك صلاحية `ads_read`
- ارجع لـ Graph Explorer وأضف الصلاحية

### ⚠️ "No ads found"
- لا توجد إعلانات لهذا المنتج
- جرب كلمة بحث أعم (مثل "سماعات" بدل "سماعات r50i")

---

## 📞 الدعم

اقرأ الدليل الشامل: [API_GUIDE.md](API_GUIDE.md)

---

## 🔒 أمان

- **لا تشارك** Access Token مع أحد
- **لا ترفع** `api_config.json` على GitHub
- **استخدم** التوكن الطويل (60 يوم) بدلاً من القصير

---

**تم بناؤه بـ ❤️ لتسهيل بحث المنتجات الرابحة في الجزائر**
