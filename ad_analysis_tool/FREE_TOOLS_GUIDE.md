# دليل الأدوات المجانية لتحليل إعلانات فيسبوك (بدون API)

بما أن Facebook Ad Library API صعب الوصول إليه، إليك 3 حلول مجانية بديلة قوية:

---

## 🔧 1. أداة فحص المنشور الفردي (Puppeteer)

أداة بسيطة تعمل على جهازك، تعطيها رابط منشور، تعطيك تحليلاً كاملاً.

**الملف:** `single_post_analyzer.js`

**كيفية التشغيل:**
1. افتح التيرمينال (Terminal).
2. تأكد من تثبيت المكتبات (مرة واحدة):
   ```powershell
   npm install puppeteer readline
   ```
3. شغّل الأداة:
   ```powershell
   node single_post_analyzer.js
   ```
4. الصق رابط المنشور واضغط Enter.

---

## 🐒 2. إضافة Tampermonkey (تحليل فوري للمتصفح)

سكربت يضيف زر "تحليل" داخل فيسبوك نفسه، يلون الإعلانات الناجحة بالأخضر والفاشلة بالأحمر.

**الملف:** `facebook_tampermonkey.user.js`

**كيفية التثبيت:**
1. حمل إضافة **Tampermonkey** من متجر Chrome.
2. انقر على أيقونة الإضافة -> "Create a new script".
3. افتح ملف `facebook_tampermonkey.user.js` الموجود في مجلدك وانسخ محتواه كاملاً.
4. الصقه في محرر Tampermonkey واحفظه (File -> Save).
5. الآن افتح فيسبوك، سترى زر "📊 تحليل الصفحة" في الزاوية.

---

## 🕷️ 3. نظام المراقبة الشامل (Scraper)

النظام الذي قمنا بإنشائه سابقاً لمراقبة صفحات متعددة.

**الملف:** `scraper.js`

**كيفية التشغيل:**
```powershell
node scraper.js
```

---

## ⚠️ ملاحظة حول `npm install`

إذا واجهت أخطاء في التثبيت سابقاً، جرب الأمر التالي لإصلاح الصلاحيات وإعادة المحاولة:

```powershell
npm cache clean --force
npm install puppeteer --no-bin-links
```
