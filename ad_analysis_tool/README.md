# 🚀 أداة تتبع المنتجات الرابحة من ميتا

أداة احترافية لأتمتة عملية اكتشاف المنتجات الرابحة من إعلانات فيسبوك وانستقرام بناءً على قاعدة ذكية.

## 📊 القاعدة الذهبية

**المنتج ناجح إذا كان: عدد التعليقات ≥ عدد الإعجابات ÷ 10**

مثال:
- الإعجابات: 1,000 👍
- التعليقات المطلوبة: 100 💬 (10%)
- إذا كان عدد التعليقات 100 أو أكثر = منتج ناجح ✅

---

## ⚡ المميزات

✅ **أتمتة كاملة** - لا حاجة للبحث اليدوي  
✅ **تتبع ذكي** - فحص آلاف الإعلانات في دقائق  
✅ **تحليل دقيق** - معايير قابلة للتخصيص  
✅ **تقارير احترافية** - CSV و Excel  
✅ **جدولة تلقائية** - تشغيل يومي/أسبوعي  
✅ **متعدد المصادر** - مكتبة الإعلانات + صفحات مباشرة  

---

## 📦 التثبيت

### 1. تثبيت Python
تأكد من تثبيت Python 3.8 أو أحدث:
```bash
python --version
```

### 2. تثبيت المكتبات
```bash
pip install -r requirements.txt
```

### 3. تثبيت Chrome Driver
البرنامج يحتاج Chrome Driver للتشغيل:

**الطريقة الآلية:**
```bash
pip install webdriver-manager
```

**الطريقة اليدوية:**
1. تحميل من: https://chromedriver.chromium.org/
2. وضعه في مجلد المشروع

---

## 🎯 الاستخدام السريع

### طريقة 1: التشغيل الأساسي
```bash
python meta_ads_tracker.py
```

### طريقة 2: النسخة المتقدمة (موصى بها)
```bash
python advanced_tracker.py
```

---

## ⚙️ الإعدادات (config.json)

### تخصيص معايير البحث:

```json
{
  "success_ratio": 0.1,
  "search_terms": [
    "dropshipping products",
    "trending products 2026",
    "viral products"
  ],
  "countries": ["US", "UK", "AE", "SA"],
  "max_ads_per_search": 50,
  "min_likes": 100
}
```

### شرح الإعدادات:

| المعامل | الوصف | القيمة الافتراضية |
|---------|-------|-------------------|
| `success_ratio` | نسبة النجاح (0.1 = 10%) | 0.1 |
| `search_terms` | كلمات البحث | قائمة |
| `countries` | الدول المستهدفة | ["US"] |
| `max_ads_per_search` | أقصى عدد إعلانات | 50 |
| `min_likes` | الحد الأدنى للإعجابات | 100 |

---

## 💡 أمثلة عملية

### مثال 1: البحث عن منتجات dropshipping
```python
from meta_ads_tracker import MetaAdsTracker

tracker = MetaAdsTracker(success_ratio=0.1)
results = tracker.scrape_meta_ads_library(
    search_term="dropshipping products",
    country="US",
    max_ads=100
)

tracker.save_results("dropshipping_winners.csv")
tracker.generate_report()
```

### مثال 2: فحص صفحة معينة
```python
tracker = MetaAdsTracker()
results = tracker.scrape_facebook_page_posts(
    page_url="https://facebook.com/yourpage",
    max_posts=50
)
```

### مثال 3: تشغيل تلقائي يومي
```python
from advanced_tracker import AdvancedMetaTracker

tracker = AdvancedMetaTracker()
tracker.schedule_scans()  # سيعمل يومياً في الساعة 9 صباحاً
```

---

## 📋 مثال على النتائج

```
📊 تقرير تحليل الإعلانات
============================================================
إجمالي الإعلانات المفحوصة: 150
الإعلانات الناجحة: 23 (15.3%)
معيار النجاح: التعليقات >= 10% من الإعجابات

🏆 أفضل 5 إعلانات:
1. ShopXYZ Store
   📊 النسبة: 25.5% | 👍 5,234 | 💬 1,335

2. TrendyProducts
   📊 النسبة: 18.2% | 👍 3,890 | 💬 708

3. ViralFinds
   📊 النسبة: 15.7% | 👍 8,120 | 💬 1,275
```

---

## 🔧 حل المشاكل الشائعة

### ❌ خطأ: Chrome Driver not found
**الحل:**
```bash
pip install webdriver-manager
```

### ❌ خطأ: Access Denied أو Blocked
**الحل:**
- استخدم VPN
- قلل سرعة الطلبات (زد `time.sleep()`)
- استخدم Facebook Graph API بدلاً من Scraping

### ❌ النتائج فارغة
**الأسباب المحتملة:**
1. فيسبوك غير محدثة الـ selectors
2. تحتاج تسجيل دخول
3. استخدم الطريقة البديلة (Graph API)

---

## 🚀 الطرق المتقدمة

### استخدام Facebook Graph API (موصى به)

#### 1. الحصول على Access Token:
- اذهب إلى: https://developers.facebook.com/tools/explorer/
- سجل دخول
- احصل على Token
- ضعه في `config.json`

#### 2. استخدام API:
```python
tracker = MetaAdsTracker(access_token="YOUR_TOKEN")
results = tracker.use_graph_api(page_id="123456789")
```

**مميزات API:**
- ✅ بيانات دقيقة 100%
- ✅ لا يتم حظرك
- ✅ سريع جداً
- ✅ بيانات تاريخية

---

## 📊 تحليل متقدم

### تحليل الاتجاهات
```python
from advanced_tracker import AdvancedMetaTracker

tracker = AdvancedMetaTracker()
trends = tracker.analyze_trends()
# يحلل جميع النتائج السابقة ويستخرج الأنماط
```

### تصدير لـ Excel
```python
import pandas as pd

df = pd.read_csv("successful_products.csv")
df.to_excel("products.xlsx", index=False)
```

---

## 🎨 تخصيص القاعدة

يمكنك تغيير معيار النجاح:

```python
# معيار صارم (5%)
tracker = MetaAdsTracker(success_ratio=0.05)

# معيار متوسط (10%) - الافتراضي
tracker = MetaAdsTracker(success_ratio=0.1)

# معيار مرن (15%)
tracker = MetaAdsTracker(success_ratio=0.15)
```

---

## 📁 هيكل الملفات

```
product-research/
│
├── meta_ads_tracker.py      # البرنامج الرئيسي
├── advanced_tracker.py      # النسخة المتقدمة
├── config.json              # الإعدادات
├── requirements.txt         # المكتبات المطلوبة
├── README.md               # هذا الملف
│
├── results_*.csv           # نتائج المسح
├── top_results_*.csv       # المنتجات الناجحة فقط
└── logs/                   # سجلات التشغيل
```

---

## ⏰ الجدولة التلقائية

### تشغيل يومي:
```python
from advanced_tracker import AdvancedMetaTracker
import json

# تعديل config.json
config = json.load(open('config.json'))
config['report_frequency'] = 'daily'
json.dump(config, open('config.json', 'w'))

# تشغيل
tracker = AdvancedMetaTracker()
tracker.schedule_scans()
```

### تشغيل كل ساعة:
```python
config['report_frequency'] = 'hourly'
```

---

## 🔐 الأمان والخصوصية

⚠️ **ملاحظات هامة:**

1. **لا تشارك Access Token** مع أحد
2. **استخدم .env** لحفظ المعلومات الحساسة
3. **احترم قوانين الاستخدام** لفيسبوك
4. **لا تفرط في الطلبات** لتجنب الحظر

---

## 📈 نصائح للنجاح

✅ **ركز على مجال واحد** (مثلاً: home decor)  
✅ **راقب الاتجاهات** الموسمية  
✅ **احفظ النتائج** بشكل دوري  
✅ **حلل المنافسين** الناجحين  
✅ **اختبر المنتجات** قبل الاستثمار الكبير  

---

## 🆘 الدعم

واجهت مشكلة؟ جرب:

1. تحقق من ملف `requirements.txt`
2. تأكد من تحديث Chrome Driver
3. جرب استخدام API بدلاً من Scraping
4. قلل عدد `max_ads_per_search`

---

## 🔄 التحديثات المستقبلية

- [ ] دعم Instagram API
- [ ] تحليل الفيديوهات
- [ ] تكامل مع TikTok
- [ ] لوحة تحكم ويب
- [ ] إشعارات تلقائية
- [ ] AI لتوقع النجاح

---

## 📜 الترخيص

هذا المشروع للاستخدام الشخصي والتعليمي.

---

## 🌟 شكراً

إذا أفادك هذا المشروع، شاركه مع الآخرين!

**حظاً موفقاً في رحلة البحث عن المنتجات الرابحة! 🚀💰**

---

## الأسئلة الشائعة (FAQ)

### س: هل يحتاج البرنامج تسجيل دخول لفيسبوك؟
ج: لا للطريقة الأساسية، نعم للـ API.

### س: كم منتج يمكن فحصه في اليوم؟
ج: مئات إلى آلاف (حسب السرعة والطريقة).

### س: هل يعمل على Instagram؟
ج: نعم، من خلال Meta Ads Library.

### س: كم تكلفة Facebook API؟
ج: مجاني للاستخدام الأساسي.

### س: هل هناك خطر الحظر؟
ج: استخدم API لتجنب ذلك، أو قلل السرعة.
