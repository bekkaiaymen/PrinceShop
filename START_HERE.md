# 🎯 ابدأ من هنا - دليل سريع

## ✅ تم التثبيت بنجاح!

جميع المكتبات المطلوبة تم تثبيتها. الآن يمكنك البدء!

---

## 🚀 الخطوة التالية: اختبار البرنامج

### الطريقة 1️⃣: البداية السريعة (موصى بها)

```powershell
python examples.py
```

ثم اختر الخيار **1** (البداية السريعة)

---

### الطريقة 2️⃣: تشغيل مثال بسيط

أنشئ ملف test_simple.py:

```python
from meta_ads_tracker import MetaAdsTracker

# إنشاء المتتبع
tracker = MetaAdsTracker(success_ratio=0.1)

print("✅ البرنامج يعمل بنجاح!")
print("🎯 القاعدة: التعليقات >= الإعجابات × 10%")
```

شغله:
```powershell
python test_simple.py
```

---

## ⚠️ ملاحظات مهمة

### 1. Chrome Driver مطلوب

البرنامج يحتاج Chrome Driver للعمل. سيتم تحميله تلقائياً عند أول تشغيل.

تأكد من تثبيت Google Chrome أولاً:
- تحميل Chrome: https://www.google.com/chrome/

### 2. تعديل الإعدادات

عدّل ملف `config.json` لتخصيص:
- كلمات البحث
- الدول المستهدفة
- نسبة النجاح (الافتراضي 10%)

---

## 🎬 مثال عملي كامل

```python
from meta_ads_tracker import MetaAdsTracker

# إنشاء المتتبع (10% معيار النجاح)
tracker = MetaAdsTracker(success_ratio=0.1)

# البحث عن منتجات
print("🔍 جاري البحث...")
results = tracker.scrape_meta_ads_library(
    search_term="dropshipping products",
    country="US",
    max_ads=20
)

# حفظ النتائج
tracker.save_results("my_results.csv")
tracker.generate_report()

print(f"✅ تم! وجدنا {len(results)} منتج ناجح")
```

---

## 📊 فهم النتائج

عند تشغيل البرنامج، ستحصل على:

1. **ملف CSV** يحتوي على:
   - اسم المعلن
   - عدد الإعجابات 👍
   - عدد التعليقات 💬
   - نسبة النجاح 📊
   - حالة (ناجح ✅ / غير ناجح ❌)

2. **تقرير نصي** يعرض:
   - عدد الإعلانات المفحوصة
   - عدد المنتجات الناجحة
   - أفضل 5 منتجات

---

## 🔧 حل المشاكل

### المشكلة: "Chrome Driver not found"
**الحل:**
```powershell
python -m pip install webdriver-manager
```

### المشكلة: "Access Denied"
**الحل:** 
- استخدم VPN
- أو استخدم Facebook Graph API

### المشكلة: لا توجد نتائج
**السبب:** 
- فيسبوك غيّر تصميم الموقع
- استخدم الطريقة البديلة (Graph API)

---

## 🎯 الخطوات القادمة

1. ✅ جرب `python examples.py`
2. ✅ عدّل `config.json` حسب احتياجاتك
3. ✅ اقرأ `README.md` للتفاصيل الكاملة
4. ✅ استخدم `advanced_tracker.py` للأتمتة الكاملة

---

## 💡 نصيحة مهمة

للحصول على أفضل النتائج:

1. **استخدم Facebook Graph API** بدلاً من Scraping
2. **احصل على Access Token** من: https://developers.facebook.com/tools/explorer/
3. **ضعه في config.json**
4. **استمتع ببيانات دقيقة 100%!**

---

## 🆘 هل تحتاج مساعدة؟

راجع:
- 📖 [README.md](README.md) - الدليل الشامل
- 💻 [examples.py](examples.py) - أمثلة عملية
- ⚙️ [config.json](config.json) - الإعدادات

---

**حظاً موفقاً في رحلة البحث عن المنتجات الرابحة! 🚀💰**
