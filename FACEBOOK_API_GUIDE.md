# 🔑 دليل إعداد Facebook Graph API

## لماذا Facebook API أفضل من Web Scraping؟

✅ **بيانات دقيقة 100%** - الإعجابات والتعليقات الحقيقية  
✅ **لا يوجد حظر** - مصرح به من فيسبوك  
✅ **سريع جداً** - استجابة فورية  
✅ **بيانات تاريخية** - تتبع الأداء عبر الزمن  
✅ **مجاني** - للاستخدام الأساسي  

---

## 📋 الخطوات التفصيلية

### الخطوة 1: إنشاء حساب Facebook Developer

1. اذهب إلى: https://developers.facebook.com/
2. سجل دخول بحسابك على فيسبوك
3. اضغط على **"Get Started"** إذا كانت أول مرة
4. أكمل التسجيل كمطور

---

### الخطوة 2: إنشاء تطبيق (App)

1. اذهب إلى: https://developers.facebook.com/apps
2. اضغط **"Create App"**
3. اختر نوع التطبيق: **"Business"**
4. املأ التفاصيل:
   - **App Name**: Product Research Tool
   - **App Contact Email**: بريدك الإلكتروني
5. اضغط **"Create App"**

---

### الخطوة 3: إضافة منتج "Facebook Login"

1. من لوحة التطبيق، ابحث عن **"Facebook Login"**
2. اضغط **"Set Up"**
3. اختر **"Web"** كمنصة
4. أدخل URL مؤقت: `http://localhost`

---

### الخطوة 4: الحصول على Access Token

#### طريقة سريعة (للاختبار):

1. اذهب إلى: https://developers.facebook.com/tools/explorer/
2. من القائمة العلوية، اختر تطبيقك
3. اضغط **"Generate Access Token"**
4. سيطلب منك الصلاحيات - اختر:
   - ✅ `pages_read_engagement`
   - ✅ `pages_show_list`
   - ✅ `read_insights`
   - ✅ `ads_read` (للإعلانات)
5. انسخ الـ **Access Token** (يبدأ بـ EAA...)

⚠️ **هذا Token مؤقت (ساعة واحدة)** - للاستخدام الطويل، انظر الخطوة 5

---

### الخطوة 5: الحصول على Long-Lived Token (يدوم 60 يوم)

#### الطريقة الأولى (عبر Graph API Explorer):

1. في Graph API Explorer: https://developers.facebook.com/tools/explorer/
2. أدخل هذا الاستعلام:

```
oauth/access_token?
  grant_type=fb_exchange_token&
  client_id={APP_ID}&
  client_secret={APP_SECRET}&
  fb_exchange_token={SHORT_LIVED_TOKEN}
```

استبدل:
- `{APP_ID}`: معرف تطبيقك (من Settings → Basic)
- `{APP_SECRET}`: سر التطبيق (من Settings → Basic)
- `{SHORT_LIVED_TOKEN}`: الـ token من الخطوة 4

#### الطريقة الثانية (عبر Python):

سأوفر لك سكريبت جاهز - انظر `get_long_lived_token.py`

---

### الخطوة 6: الحصول على Page Access Token (للصفحات)

إذا تريد تتبع صفحة معينة:

1. في Graph API Explorer
2. اختر الـ Token الخاص بك
3. أدخل:
```
/me/accounts
```
4. ستحصل على قائمة بصفحاتك مع tokens خاصة بكل صفحة

---

## 🔧 إعداد البرنامج

### 1. احفظ الـ Token

افتح `config.json` وضع الـ Token:

```json
{
  "facebook_access_token": "EAAxxxxxxxxxxxxxxxxxxxxx",
  "facebook_app_id": "123456789",
  "facebook_app_secret": "xxxxxxxxxxxxxx"
}
```

⚠️ **لا تشارك هذه المعلومات مع أحد!**

---

### 2. استخدم ملف .env (أكثر أماناً)

انسخ `.env.example` إلى `.env`:

```bash
copy .env.example .env
```

افتح `.env` وأضف:

```
FACEBOOK_ACCESS_TOKEN=EAAxxxxxxxxxxxxx
FACEBOOK_APP_ID=123456789
FACEBOOK_APP_SECRET=xxxxxxxxxxxxxx
```

---

## 🧪 اختبر الـ API

شغّل السكريبت المساعد:

```bash
python test_facebook_api.py
```

سيختبر:
- ✅ صلاحية الـ Token
- ✅ الصلاحيات المتاحة
- ✅ مدة صلاحية الـ Token
- ✅ القدرة على قراءة البيانات

---

## 📊 ماذا يمكنك الحصول عليه من API؟

### 1. بيانات الصفحات (Pages):
- عدد المتابعين
- معدل التفاعل
- أفضل المنشورات

### 2. بيانات المنشورات (Posts):
- عدد الإعجابات الحقيقي
- عدد التعليقات
- عدد المشاركات
- الوصول (Reach)
- نسبة التفاعل

### 3. بيانات الإعلانات (Ads):
- الإعلانات النشطة
- الأداء (Insights)
- الميزانية المنفقة
- معدل التحويل

---

## 🎯 أمثلة API Endpoints

### الحصول على معلومات صفحة:
```
GET https://graph.facebook.com/v18.0/{PAGE_ID}
?fields=name,followers_count,engagement
&access_token={TOKEN}
```

### الحصول على منشورات صفحة:
```
GET https://graph.facebook.com/v18.0/{PAGE_ID}/posts
?fields=message,likes.summary(true),comments.summary(true),shares
&access_token={TOKEN}
```

### الحصول على insights منشور:
```
GET https://graph.facebook.com/v18.0/{POST_ID}/insights
?metric=post_impressions,post_engaged_users
&access_token={TOKEN}
```

### البحث عن صفحات:
```
GET https://graph.facebook.com/v18.0/pages/search
?q=dropshipping
&type=page
&access_token={TOKEN}
```

---

## 🔐 الصلاحيات المطلوبة (Permissions)

للاستخدام الكامل، احتاج:

### للصفحات العامة (لا تحتاج موافقة):
- ✅ `pages_read_engagement`
- ✅ `pages_show_list`

### للإعلانات (تحتاج موافقة Facebook):
- 🔒 `ads_read`
- 🔒 `ads_management`

### للبيانات التفصيلية (تحتاج موافقة):
- 🔒 `read_insights`

---

## ⚠️ القيود والحدود (Rate Limits)

Facebook لديه حدود على عدد الطلبات:

- **200 طلب/ساعة** لكل مستخدم
- **4800 طلب/ساعة** لكل تطبيق

البرنامج يتعامل مع هذه القيود تلقائياً.

---

## 🆘 حل المشاكل الشائعة

### ❌ خطأ: "Invalid OAuth access token"
**الحل:** الـ Token منتهي الصلاحية - احصل على واحد جديد

### ❌ خطأ: "Permissions error"
**الحل:** تحتاج صلاحيات إضافية - راجع الخطوة 4

### ❌ خطأ: "Rate limit exceeded"
**الحل:** انتظر ساعة أو قلل عدد الطلبات

### ❌ خطأ: "App not approved"
**الحل:** للصلاحيات المتقدمة، تحتاج موافقة Facebook

---

## 🎓 موارد إضافية

- 📚 **توثيق API**: https://developers.facebook.com/docs/graph-api
- 🧪 **Graph API Explorer**: https://developers.facebook.com/tools/explorer/
- 💬 **مجتمع المطورين**: https://developers.facebook.com/community/
- 📹 **فيديوهات تعليمية**: ابحث عن "Facebook Graph API tutorial"

---

## ✅ الخطوة التالية

بعد إعداد الـ API:

```bash
python api_tracker.py
```

سيعطيك بيانات دقيقة 100% مع تطبيق القاعدة تلقائياً!

---

**مبروك! الآن لديك أقوى أداة لتتبع المنتجات الرابحة! 🚀**
