# تعليمات رفع المشروع إلى GitHub 📤

## الخطوة 1️⃣: إنشاء Repository على GitHub

### من خلال الموقع:
1. افتح [GitHub.com](https://github.com)
2. اضغط على زر **"+"** في الأعلى → **"New repository"**
3. املأ البيانات:
   - **Repository name**: `affiliate-marketing-ghardaia`
   - **Description**: `نظام تسويق بالعمولة لبيع إكسسوارات الهواتف في غرداية - Affiliate Marketing System`
   - **Public** أو **Private** (اختر حسب رغبتك)
   - ❌ **لا تفعّل** "Initialize with README" (لأننا نملك الكود بالفعل)
4. اضغط **"Create repository"**

## الخطوة 2️⃣: ربط المشروع المحلي بـ GitHub

افتح PowerShell في مجلد المشروع ونفذ الأوامر التالية:

### استبدل `YOUR_USERNAME` باسم المستخدم الخاص بك على GitHub

```powershell
# ربط Repository المحلي بـ GitHub (استبدل YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/affiliate-marketing-ghardaia.git

# التحقق من الربط
git remote -v

# رفع الكود إلى GitHub
git push -u origin main
```

### إذا ظهرت رسالة خطأ حول Branch name:

```powershell
# تغيير اسم الـ branch إلى main
git branch -M main

# ثم حاول الرفع مرة أخرى
git push -u origin main
```

## الخطوة 3️⃣: إدخال بيانات GitHub

عند الرفع لأول مرة، سيطلب منك GitHub:
- **Username**: اسم المستخدم على GitHub
- **Password**: استخدم **Personal Access Token** بدلاً من كلمة المرور

### كيفية إنشاء Personal Access Token:

1. اذهب إلى [GitHub Settings → Developer settings](https://github.com/settings/tokens)
2. اضغط **"Generate new token"** → **"Generate new token (classic)"**
3. املأ البيانات:
   - **Note**: `Affiliate Marketing Upload`
   - **Expiration**: اختر مدة الصلاحية
   - **Scopes**: فعّل ✅ **repo** (كامل)
4. اضغط **"Generate token"**
5. **انسخ التوكن** واحفظه في مكان آمن (لن تستطيع رؤيته مرة أخرى!)

استخدم هذا التوكن كـ **Password** عند الرفع.

## الخطوة 4️⃣: التحقق من النجاح ✅

بعد الرفع بنجاح:
1. افتح Repository على GitHub
2. تأكد من ظهور جميع الملفات
3. تحقق من ظهور ملف [README_AR.md](README_AR.md) بشكل صحيح

## 📋 أوامر Git الأساسية للاستخدام المستقبلي

### بعد تعديل الملفات:

```powershell
# إضافة جميع التغييرات
git add .

# عمل commit مع رسالة وصفية
git commit -m "وصف التعديلات هنا"

# رفع التغييرات إلى GitHub
git push
```

### جلب آخر التحديثات من GitHub:

```powershell
git pull
```

### معرفة حالة الملفات:

```powershell
git status
```

### عرض سجل التغييرات:

```powershell
git log --oneline
```

## 🔐 ملاحظات أمان مهمة

✅ **تم استبعاد الملفات الحساسة تلقائياً:**
- ❌ `.env` (المتغيرات البيئية)
- ❌ `service-account-key.json` (مفاتيح Google Cloud)
- ❌ `node_modules/` (المكتبات)
- ❌ `*.pdf` (ملفات PDF الكبيرة)

⚠️ **لا ترفع أبداً:**
- كلمات المرور
- مفاتيح API
- معلومات قاعدة البيانات
- بيانات العملاء

## 🐛 حل المشاكل الشائعة

### المشكلة: `error: src refspec main does not exist`
**الحل:**
```powershell
git branch -M main
git push -u origin main
```

### المشكلة: `rejected - non-fast-forward`
**الحل:**
```powershell
git pull origin main --rebase
git push
```

### المشكلة: `Permission denied`
**الحل:**
- تأكد من استخدام **Personal Access Token** بدلاً من كلمة المرور
- تحقق من صلاحيات التوكن

### المشكلة: حجم الملفات كبير جداً
**الحل:**
```powershell
# إضافة ملفات الصور إلى .gitignore إذا كانت كبيرة جداً
echo "frontend/public/products_clean/*.jpeg" >> .gitignore
git add .gitignore
git commit -m "Exclude large image files"
```

## 📞 الدعم

إذا واجهتك أي مشكلة:
1. راجع رسائل الخطأ بعناية
2. ابحث عن الحل على [GitHub Docs](https://docs.github.com)
3. اسأل في [GitHub Community](https://github.community)

---

**صُنع بـ ❤️ في غرداية، الجزائر 🇩🇿**
