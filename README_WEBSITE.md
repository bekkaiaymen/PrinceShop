# 🚀 منصة التسويق بالعمولة - MERN Stack

## 📋 المميزات

✅ **عرض احترافي للمنتجات** مع صور تلقائية  
✅ **استخراج صور من الإنترنت** (Unsplash API + Placeholder)  
✅ **تصنيف تلقائي** للمنتجات حسب الاسم  
✅ **بحث وفلترة** متقدمة  
✅ **عرض الأرباح** بشكل واضح للمسوقين  
✅ **نسخ معلومات المنتج** بضغطة واحدة  
✅ **واجهة عربية** كاملة RTL  
✅ **تصميم responsive** لجميع الأجهزة  

---

## 📦 التثبيت والإعداد

### 1️⃣ تثبيت MongoDB

**Windows:**
1. حمّل من: https://www.mongodb.com/try/download/community
2. ثبّت البرنامج
3. شغّل MongoDB:
```powershell
mongod
```

**أو استخدم MongoDB Atlas (Cloud - مجاني):**
- سجّل في: https://www.mongodb.com/cloud/atlas/register
- أنشئ Cluster مجاني
- احصل على Connection String
- ضعه في `.env`: `MONGODB_URI=mongodb+srv://...`

---

### 2️⃣ تثبيت Dependencies

```powershell
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

---

### 3️⃣ إعداد Unsplash API (اختياري - للصور الحقيقية)

1. سجّل مجاناً: https://unsplash.com/developers
2. أنشئ Application جديد
3. انسخ Access Key
4. ضعه في `backend/.env`:
```env
UNSPLASH_ACCESS_KEY=your_key_here
```

**ملاحظة:** إذا لم تضع API Key، سيستخدم النظام صور placeholder تلقائياً.

---

### 4️⃣ استيراد البيانات من Google Sheets

```powershell
cd backend
npm run import
```

**سيقوم بـ:**
- قراءة البيانات من Google Sheets
- تصنيف المنتجات تلقائياً
- استخراج صور لكل منتج
- حفظ كل شيء في MongoDB

---

## 🚀 تشغيل الموقع

### Terminal 1 - Backend:
```powershell
cd backend
npm run dev
```
سيعمل على: http://localhost:5000

### Terminal 2 - Frontend:
```powershell
cd frontend
npm run dev
```
سيفتح على: http://localhost:3000

---

## 📊 API Endpoints

### المنتجات:
- `GET /api/products` - جميع المنتجات (مع filters)
- `GET /api/products/:id` - منتج واحد
- `GET /api/products/sku/:sku` - منتج بـ SKU

### الفئات:
- `GET /api/categories` - جميع الفئات

### الإحصائيات:
- `GET /api/stats` - إحصائيات عامة

---

## 🎨 التخصيص

### تعديل الألوان:
في `frontend/src/index.css`:
```css
background: linear-gradient(135deg, #your-color-1 0%, #your-color-2 100%);
```

### تعديل نسبة الربح:
في `importProducts.js` السابق كانت 1.4 (40% ربح)

### إضافة فئات جديدة:
في `backend/importFromSheets.js` - دالة `detectCategory()`

---

## 📱 المميزات للمسوقين

1. **نسخ سريع** لمعلومات المنتج
2. **عرض الربح واضح** لكل منتج
3. **حساب نسبة الربح** تلقائياً
4. **بحث سريع** عن المنتجات
5. **فلترة حسب الفئة**
6. **واجهة سهلة** للجوال

---

## 🔧 الملفات المهمة

```
affiliate marketing/
├── backend/
│   ├── models/Product.js          # نموذج المنتج
│   ├── server.js                  # API Server
│   ├── importFromSheets.js        # استيراد + صور
│   └── .env                       # الإعدادات
├── frontend/
│   ├── src/
│   │   ├── pages/HomePage.jsx     # الصفحة الرئيسية
│   │   ├── pages/ProductPage.jsx  # صفحة المنتج
│   │   └── components/            # المكونات
│   └── index.html
└── service-account-key.json       # مفتاح Google
```

---

## 🎯 الخطوات التالية

✅ الموقع جاهز للاستخدام!

**تحسينات مستقبلية:**
- إضافة نظام تسجيل دخول للمسوقين
- تتبع المبيعات والعمولات
- لوحة تحكم للإدارة
- API لتطبيق الجوال
- نظام الإشعارات

---

## ❓ حل المشاكل

**MongoDB لا يعمل:**
```powershell
# تأكد من تشغيل MongoDB
mongod
```

**الصور لا تظهر:**
- تحقق من Unsplash API Key
- أو استخدم placeholder (يعمل تلقائياً)

**خطأ في الاستيراد:**
- تأكد من `service-account-key.json` موجود
- تحقق من صلاحيات Google Sheets

---

## 📞 الدعم

إذا واجهت أي مشكلة، أخبرني!

**الموقع جاهز 100% للاستخدام! 🎉**
